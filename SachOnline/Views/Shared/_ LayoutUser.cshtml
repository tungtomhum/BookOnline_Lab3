<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8" name="viewport" content="width=device-width, initial-scale=1">
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <script src="~/Scripts/bootstrap.min.js"></script>
    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style type="text/css">
        .imgbook {
            transition: all 1s ease-in;
            width: 400px;
        }

            .imgbook:hover {
                transform: scale(0.9);
                cursor: pointer;
            }

        .imgsachbannhieu {
            width: 100px;
        }

        .thumbnail {
            height: 520px;
        }

            .thumbnail img {
                height: 380px;
            }

        .ten {
            height: 50px;
            text-transform: uppercase;
        }
    </style>

    <title>@ViewBag.Title</title>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-lg-3 col-md-3 col-sm-3 col-xs-12">
                <div>
                    <a href="~/SachOnline/Index" class="btn btn-light">HOME</a>
                    <a href="~/User/DangKy" class="btn btn-light">REGISTER</a>
                    <a href="~/User/DangNhap" class="btn btn-light">LOGIN</a>
                </div>
                @Html.Action("ChuDePartial", "SachOnline")
                @Html.Action("NhaXuatBanPartial", "SachOnline")
            </div>
            <div class="col-lg-9 col-md-9 col-sm-9 col-xs-12">
                @RenderBody()
                @Html.Action("SachBanNhieuPartial", "SachOnline") 
            </div>
        </div>
    </div>
</body>


</html>
